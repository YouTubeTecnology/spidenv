<!doctype html>
<html lang="it">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>SPID & CIE Demo</title>
    <!-- Bootstrap Italia CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-italia@2.8.3/dist/css/bootstrap-italia.min.css" rel="stylesheet">
</head>

<body>
    <div class="it-header-wrapper">
        <div class="it-header-slim-wrapper">
            <div class="container">
                <div class="row">
                    <div class="col-12">
                        <div class="it-header-slim-wrapper-content">
                            <a class="d-lg-block navbar-brand" href="/">Dipartimento Protezione Civile</a>
                            <span class="nav-item">
                                <% if (typeof user !=='undefined' && user) { %>
                                    <a class="nav-link" href="/logout">Esci</a>
                                    <% } else { %>
                                        <a class="nav-link" href="/login">Accedi</a>
                                        <% } %>
                            </span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <main class="container my-5">